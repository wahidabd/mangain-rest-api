openapi: '3.0.2'
info:
  title: API Title
  version: '1.0'
servers:
  - url: http://localhost:5000

paths:
  /api/home:
    get:
      responses:
        '200':
          description: OK

  /api/kiryuu-new-update/{page}:
    get:
      description: ''
      parameters:
        - in: path
          name: page
          required: true
          schema:
            type: integer
            minimum: 1
      
      responses:
        '200':
          description: OK
